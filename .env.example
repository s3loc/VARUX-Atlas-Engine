# VARUX Atlas Engine environment template
# Bu dosyayı kopyalayıp .env olarak güncelleyin ve gizli bilgileri asla commit etmeyin.

# API & AI
OPENAI_API_KEY=changeme-openai-key
VARUX_OPENAI_MODEL=gpt-4o-mini
VARUX_AI_API_KEY=${OPENAI_API_KEY}

# Database
VARUX_DB_URL=postgresql+psycopg2://varux:strongpassword@localhost:5432/varux

# OAuth / Authentication
VARUX_OAUTH_CLIENT_ID=changeme-client-id
VARUX_OAUTH_CLIENT_SECRET=changeme-client-secret
VARUX_OAUTH_CALLBACK_URL=http://127.0.0.1:8050/auth/callback
VARUX_SESSION_SECRET=use-a-long-random-string

# File system & config
VARUX_BASE_DIR=.
VARUX_CONFIG_PATH=~/.varux/config.yaml

# External integrations (optional)
VAULT_TOKEN=
SIEM_AUTH_TOKEN=
